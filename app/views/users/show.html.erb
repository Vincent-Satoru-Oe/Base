<% provide(:title, @user.name) %>

<div class= "row">
	
	<aside class="span4">
		<section>
			<h1>
				<%= gravatar_for(@user, size: 50) %>
				<h1>
					<%= @user.name %>
					<br>
					<%= @user.email %>
				</h1>
			</h1>
		</section>
	</aside>
	
</div>
